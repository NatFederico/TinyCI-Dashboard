export class Board {
    id: string;
    name: string;
    hub: string;

    constructor(id: string, name: string, hub: string) {
        this.id = id;
        this.name = name;
        this.hub = hub;
    }
}
